[build]
  builder = "RAILPACK"
  buildCommand = "uv sync --frozen --extra all --extra messaging --extra anthropic --extra bedrock --extra azure-identity --extra hindsight --extra matrix"

[deploy]
  startCommand = "python -m hermes_cli.main gateway run --replace"
  healthCheckPath = "/health"
  healthCheckTimeout = 300
